{% extends 'user/FrontEnd/basefHome.html.twig' %}

{% block title %} Sante 180° {% endblock %}

{% block body %}
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">{{ 'Sign in'|trans }}</div>
                <div class="card-body">
                    <form method="post" action="{{ path('login') }}">
                        {{ form_row(form.email) }}
                        {{ form_row(form.mdp) }}
                        
                        <button type="submit" class="btn btn-primary">{{ 'Sign in'|trans }}</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
